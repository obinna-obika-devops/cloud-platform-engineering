replicaCount: 2
image:
  repository: ghcr.io/obinna-obika-devops/platform-demo
  tag: "1.0.0"
  pullPolicy: IfNotPresent
service:
  port: 8080
resources:
  requests: { cpu: 100m, memory: 128Mi }
  limits: { cpu: 500m, memory: 512Mi }
autoscaling:
  minReplicas: 2
  maxReplicas: 10
  targetCPUUtilizationPercentage: 70
