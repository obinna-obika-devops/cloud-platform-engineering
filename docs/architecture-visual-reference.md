# Visual Reference

`architecture-overview.svg` is the canonical visual architecture asset for this repository.

It is a static SVG and can be rendered directly by GitHub or embedded in portfolio documentation.