# Architecture Visual Summary

See `architecture-overview.svg` for the canonical standalone diagram.

Lifecycle: Developer → GitHub → CI → Terraform/GitOps → AWS/Kubernetes → Security + Observability → SRE → Feedback.