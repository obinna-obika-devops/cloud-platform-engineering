# Architecture Reference

The primary visual is `architecture-overview.svg`.

Engineering lifecycle: Developer → GitHub → CI → Terraform/GitOps → AWS/Kubernetes → Workloads → Security/Observability → SRE Operations → Feedback.

This is a portfolio/reference architecture, not a production deployment claim.